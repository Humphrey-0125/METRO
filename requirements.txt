# Core ML / numeric
numpy==1.24.4
scikit-learn==1.3.2

# Clustering (optional — only needed if using HDBSCAN)
# hdbscan>=0.8.33
# GPU KMeans (optional — requires CUDA)
# cuml>=23.0
# cupy==13.6.0

# Streaming JSON (optional — speeds up large file parsing)
ijson==3.3.0

# LLM API clients
openai>=1.0.0
httpx>=0.24.0
requests>=2.32.0
tenacity>=8.2.0

# Environment & utilities
python-dotenv>=1.0.0
filelock>=3.12.0
tqdm>=4.67.0
